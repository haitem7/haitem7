Photographs go here. See the table in the repository README for the exact
filenames, aspect ratios and suggested export sizes. Until a file exists,
its slot renders as a deep olive gradient panel rather than a broken image.
